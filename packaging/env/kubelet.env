# Environment for kubelet.service. Flags also bind these env vars directly
# (clap env): APISERVER_URL, NODE_NAME, CRI_SOCKET, POD_CIDR, CNI_CONF_DIR.
APISERVER_URL=http://127.0.0.1:6443
# Default: CRI-O over gRPC (socket auto-detected, /run/crio/crio.sock first).
KUBELET_ARGS=--runtime=cri
